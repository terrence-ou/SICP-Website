---
import { menuLinks } from "../site.config";
---

<header class="flex items-center justify-between">
  <div class="flex">
    <div></div>
    <div class="flex flex-col">
      <h1>SICP-JS solutions</h1>
      <nav>{menuLinks.map((link) => <a href={link.path}>{link.title}</a>)}</nav>
    </div>
  </div>
  <div>theme</div>
</header>
